<UserControl x:Class="Calculator.View.ResultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" DataContext="{Binding Source={StaticResource Locator}, Path=ResultViewModel}">
    <DockPanel>

        <Label Content="계산 값 : " HorizontalAlignment="Left" VerticalAlignment="Center"/>
        <Label x:Name="labelResult" HorizontalAlignment="Left" VerticalAlignment="Center" Content="{Binding Result}"/>
    </DockPanel>
</UserControl>
